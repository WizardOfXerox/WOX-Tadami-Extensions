ext {
    extName = 'Anikage'
    extClass = '.Anikage'
    extVersionCode = 2
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:playlistutils'))
}

